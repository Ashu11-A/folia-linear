# Level 15 — baseline × 3 + rc2 × 3 + 2 SIGKILL rows — IN PROGRESS

- `test=15` `level_confirmed=15` (3-source: config `compression-level: 15` in `paper-world-defaults.yml` + header audit 8382/8382 lvl=15 at offset 17 + 0 `[region-format]` fallback lines; baseline panel has no `lvl` token per paper-0010, rc token pending)
- `jar=baseline` `jar_sha256=e609c1d439e7bdd68e68abccbcf02c818efe77fa4ae8d7ddc2914204ca0ee06b` (60532041 B, host `stress/jars/folia-baseline.jar` + shadow-server identical, verified 2026-09-24)
- `jar=rc2` `jar_sha256=b0ea20484d85e4adce697cb786bf164b6fa4865d9eb122b8c931910e2723e1d6` (60541066 B, host `stress/jars/folia-rc2.jar`, verified 2026-09-24)
- `ldm=0` `flush_threads=1` `compression_workers=0` (axis-1 defaults)
- `bytes_anvil=26056405704` (frozen constant, S2-finalize 2026-09-22; NOT re-derived, source-anvil read-only)
- `convert_s=2354` (t0 06:13:39Z→t1 06:52:53Z; per-dim walls 2174.6+45.3+126.5=2346.4s, threads 4/2/2, empty `level-15/` dir, ≤8 threads)
- `bytes_on_disk=13810742340` (≈12.86 GiB; OW 13492471232 + nether 130361788 + end 187909210 out-bytes; per-folder `du -sb` minus manifests 1999203+31751+239264; ratio vs Anvil 0.5300, 47.00% saved; saved vs #1 22.42%)
- Conversion (`mca2linear -c 15` per-dim via host helper `level15-convert` on bind-mounted build volume, python:3.13-slim + pyzstd): OW 7382 files in=23318872064 out=13492471232 ratio=0.579 wall=2174.6s errors=1 (r.14.16, see anomaly A1); nether 117 files in=274874368 out=130361788 ratio=0.474 wall=45.3s errors=0; end 884 files in=2462646272 out=187909320 ratio=0.076 wall=126.5s errors=0; all `EXIT=0` (exit code does NOT reflect per-file errors — tooling quirk, Loop-5 input); 0 `.mcc`; free 27G→14G (floor 14≥8 PASS)
- Verify (decode-back workaround, harness `verify_level` broken by construction): header audit 8382/8382 lvl=15 bad=0 (offset 17, `>QBQbhI` field 4); manifest src_sha cross-check 8381/8382 match + 1 disputed (r.-17.-8, see anomaly A2) + 1 error entry (r.14.16, see A1); forceload-subset (16 pristine files) `linear2mca` + `verify` per-dim 12/2/2 files all `total diffs=0` (temp mini dirs wiped)
- Pristine: 16 files (OW 4×(region+entities+poi)=12 + nether 2 + end 2) 23266480 B + `pristine.sha256` 16 lines; `EXPECTED.json` 6144 keys (edit-workload.py gen --level 15, sha `e2756e46…`, scheme `sha256("dim|cx|cz|mat|level")` verified on 2 sample keys) + `commands-y319.txt` 6144 setblocks (4096 OW @319, zero 320) + `commands.txt` copy staged (mirror `level-15/`, host copies sha-verified)
- Staging (move, not copy — same `/dev/sda6`, rename instant): backed up small world + configs to `level-15/smp-test-small-backup`; moved 8 dirs `level-15/tree/{dims}/{region,entities,poi}` → `smp-test/.../dimensions/minecraft/...` (world region counts OW 2501+1miss / nether 64 / end 676 — r.14.16.region absent, r.-17.-8.region present-but-disputed); `TREE` left with manifests only; set `compression-level: 6→15`, `log-flush-batches: false→true`; appended NMT to `sexidium-node.args`. Explicit per-dim `mv` commands (single-quoted SSH, no loop vars — L6 staging lesson honored)
- Smoke boot baseline `Done (...)`, 0 fallback lines, config-readback level 15 → `level_confirmed=15`; console loopback `linearstats` → baseline panel (dirty 0, C4 as expected); smc.py `bff98d8c` + smc_batch.py `a2b7376b` (persisted L6 layer, shas re-verified); clean SIGTERM stop (143) afterwards, container STOPPED for b1

## Anomalies (conversion-time, pre-run)

- A1 SOURCE-CORRUPT r.14.16.mca (OW region, chunks 448–479 × 512–543): converter `ERROR converting ... Error -3 incorrect data check`, `errors=1`, no `.linear` emitted (deterministic — standalone retry reproduces). Independent diag: 1024/1024 chunks present, exactly ONE bad (slot 517 → chunk (453,528), ctype=2 zlib, 7804 B). mtime 2026-09-19 (backup era — corruption predates the campaign; L1's OW DONE line is unlogged so no level proves it clean). No recovery path (pre-Linear tarball deleted in Immediate-Actions; 08-11 worlds.tgz wrong era). Region far outside test geometry (forceload 0–63, pristine r.0.0–r.1.1). Tree stages WITHOUT it (8382 files on disk, 8381 staged + r.-17.-8 disputed). Size anchor notes the hole. Loop-5 input.
- A2 UNSTABLE-READ r.-17.-8.mca (OW region, chunks −544..−513 × −256..−225): manifest src_sha `5576aaef…` ≠ current stable bytes `0b12342a…` (7+ reads agree); standalone re-parse of current bytes fails slot 255 (`invalid literal/lengths set`); BUT the emitted `.linear` decodes cleanly and per-chunk decoded-vs-current-source comparison gives 1023/1024 match (only slot 255 differs: decoded=valid payload, current=unparseable). Conclusion: convert-time read was self-consistent (zlib-valid whole region — random corruption essentially never is), current read is the degraded one; output very likely TRUE data. STAGED with dispute disclosed (never dirtied by workload, never enters shutdown drain, zero measurement impact either way). mtime 2026-09-19 (untouched). Possible host read-path instability — cross-ref agent-7 memory-corruption finding (memtest never run). Loop-5 forensics input.
- A3 converter exit code 0 despite `errors=1` (`sys.exit(2 if err else 0)` not observed — EXIT-OW 0). DONE-line `errors=` is authoritative, not the exit code. Loop-5 tooling input.
- Pre-existing: `stress/convert.lock/L3` stale dir (2026-09-23, L3 agent leftover) — untouched, harmless; L15 uses its own lock path discipline (standing `sleep 200000` reservation, no second lock).
- A4 STAGING nesting (own incident, fixed 07:08Z): `mv SRC DST` with DST=pre-existing small-world dir nests (5 region/entities dirs got `region/region` + small files mixed in; 2 poi dirs + nether/entities had no small dir → clean rename). Fixed by `rm *.linear/*.tmp` at top + lifting nested dirs up + rmdir. Post-fix counts EXACT vs convert manifest: OW 2501+2500+2380=7381 / nether 64+53=117 / end 676+144+64=884 (total 8382; OW region 2501 = 2502 − r.14.16). No small-world byte remains in the staged tree (small world intact in `smp-test-small-backup` 1086297 B). Lesson for #18: `rm -rf` dest subdirs before `mv` (p0run.stage_world does this; manual staging must too).

## Runs (STOPPED — 1/8 rows: b1 baseline ❌; NO FURTHER RUNS — see verdict)

| run | boot_s | load_s (+evidence grade) | save_all_s | flush p50/p99 | dirty_at_stop | shutdown_s | exit/oom | tick_p99 | mem steady/peak | chunks lost | free± |
|---|---|---|---|---|---|---|---|---|---|---|---|
| b1 (baseline ❌) | 10.0 | 351.9 (per-chunk; batched driver 21×300, staged EDITs; 3364 head-start, +300/batch to 6144 at batch 11; C6 future-skipped=0) | n/a (never-invoked; 90s cap, movement=false; 03-H2/H3) | -/- (baseline absent, correct) | 55 | 121.0 ❌ (inferred Stopping 08:21:18→Finished 08:23:19; driver died before docker-wait; park-stop 120.9/137 confirms) | 137/0 (presumed SIGKILL grace-expiry; driver died before inspect; park-stop 137/0 confirms; 0 `Failed to flush`, 1 `.tmp` orphan, no clean markers) | -1 (mspt-no-response; 0.0ms filtered, 0 pairs) | heap -/- rss -/- nmt -/- cgroup -/- (series lost, driver died mid-shutdown-wait; prestop single heap 1714/r4975? see sidecars; abort-spam proves cap touched) | -1/6144 (unknown — verify not done before restart, driver died; tree now double-killed b1+park; S2 tail-loss expected but unmeasured) | 14→14 (no breach) |

## Run b1 (baseline) — ❌ STOP 2026-09-24 08:00:54Z→08:23:19Z (driver `loop4-P0-mirror/l15wrap.py b1`; orphaned driver died mid-shutdown-wait, log ends 08:23:01; loopback console)

- Gates pre-run: free **14 GiB ≥ 8 floor** (abort <8 never triggered); lock = standing `sleep 200000` PID 3159562 (no second lock, no break); jar baseline `e609c1d4` (shadow + node); config `compression-level: 15` + globals threads 1/workers 0/LDM 0/freq 10/log-batches true; NMT **ON**; pristine 16/16 sha OK (double restore: pre-run unmeasured-stop restore + `run_axis` restore); EXPECTED-15 6144 keys + `commands-y319.txt`; plugin `21997aad`; `smp1_players=-1` (live STOPPED by owner); `nbtlib` local.
- Boot: restore 08:02:07Z → start 08:02:14Z → `Done (9.999s)` → `boot_s=9.999`; baseline plain panel + config 15 + 0 fallback → `level_confirmed=15`.
- Load: forceload 24-add recipe → query **4096/1024/1024 exact**; pass 1 staged 6144 EDITs in 21×300 batches → `load_s=351.9` (batch-1 acks 3364 head-start, +300/batch to 6144 at batch 11 proves fresh; C6 future-skipped=0); pass 2 redirty identical setblocks → all 21 BATCH_DONE, `load_s=-354.8` DISCARDED by design (pass-2 timing never enters CSV; negative from stale-tail refresh, same mechanism as L1 r2 C6).
- Age-wait (autosave-watch, run-1 baseline): 15s + 65s, flush 0→0 + dirty 55→55 → `autosave_reaches_flush=false` (S1 reproduced on L15).
- Save-all: `save-all flush` POST → `{"ok":true}`; 90s capped poll, NO completion line; movement=false → `save_all_s=n/a (never-invoked)` per spec 33 §7 (C3 reproduced).
- Flush: baseline plain panel → `flush_p50_ms=-1`, `flush_p99_ms=-1` (absent, correct); no batch lines (expected).
- Shutdown ❌: SIGTERM 08:21:18Z → overworld `Saved 1581 block chunks in 91.23s` at 08:22:50Z → nether `Saving chunks around [-13,16]` at 08:22:50Z → NO further clean markers → container FinishedAt 08:23:19Z (121s after Stopping) → auto/manual restart StartedAt 08:23:22Z → server `Starting` 08:23:29Z (`Done 10.216s` verify-reboot). Gap 08:21:18→08:23:29 = 131s (120s grace + 11s boot) = SIGKILL signature per spec 34 §4 (clean 8–12s). `shutdown_s=121.0` inferred (1s log resolution, driver died before `docker wait`; see park confirm). 1 `.linear.tmp` orphan post-kill (`the_nether/region/r.0.-1.linear.tmp` — kill-mid-flush signature per spec 38 §5, recorded not a failure; explains G6-tree 65 vs staged 64). 0 `Failed to flush`, no fallback SEVERE.
- Driver death (honest, no data impact on verdict): `l15-b1.runner.log` ends 08:23:01Z mid-shutdown-wait (MEM ABORT spam); no local python driver survives (`ps -ef` clean, `lsof` no FRP conn); mem-series + tick-cooked + verify never finished (series lost); samples/15-baseline-1/ holds EDITLOGs (6144 dicts), EXPECTED, panels, stats, mem-boot/prestop singles, mspt, save, tail-stop (283K) — driver writes mirror-direct, no separate sync needed. Prior dead attempt `l15-b1-dead-0730Z` (07:08Z, same shape, EDIT 352.6s, autosave false, second EDIT -354.6s discarded) preserved aside, contributes no row.
- Tick: `mspt` 0.0/0.0/0.0 responses filtered (0 miss, 0 pairs) → `tick_p99_ms=-1` reason `mspt-no-response` (same as all prior).
- Mem: series LOST (driver died); prestop single `heap 1714/r4975?` + abort-spam proves cgroup ≥95% touched (same precedent as L1/L3/L6/L9 flag-only); CSV keeps `-1` unknowns (renderer `n/a`), honest.
- Safety: `flush_failures=0`; `level_fallback_severe=0`; `chunks_lost=-1` (unknown — post-shutdown fetch BEFORE restart never happened, driver died; tree rebooted 08:23:22Z then park-killed 08:42:39Z→08:44:40Z, now double-killed; S2 tail-loss expected but unmeasured; Loop-5 input).
- Disk: `free_before_gib=14` → `free_after_wipe_gib=14` (no wipe for STOP — tree + `.tmp` preserved for evidence; floor holds).
- Sidecars: mirror `samples/15-baseline-1/` (EDITLOGs, EXPECTED, panels, stats, mem singles, mspt, save, tail-stop) + `samples/15-baseline-1-dead-0730Z/` (prior attempt) + `samples/15-baseline-1-parkstop/` (tail-park, park-stop confirmation) — driver writes mirror-direct.
- Conversion disclosure (already valid): `-c 15` (r.14.16 absent A1, r.-17.-8 disputed A2, both outside forceload 0–63 / pristine r.0.0–r.1.1 geometry — zero measurement impact either way).

## Confirmation (park stop, unmeasured, no data at stake) — 2026-09-24 08:42:39Z→08:44:40Z

- Post-b1 idle server (L15 tree, post-kill + verify-reboot, dirty 53 from 08:40:42 linearstats) SIGTERM with 120s grace → **shutdown_s=120.9s, `ExitCode=137`, `OOMKilled=false`** (Portainer `stop`+`wait` BEFORE any restart; `inspect Running=False Exit=137 OOM=False`). Tail: `Stopping` 08:42:40, overworld `Saved 1342 block chunks in 88.54s` at 08:44:08, nether `Saving [-13,16]` at 08:44:08, NO clean markers (`Saved all worlds` / `All RegionFile` absent) — killed mid-nether, identical shape to b1 (91s vs 88s overworld). Container left STOPPED.
- 2/2 reproducible: b1 inferred 121.0s (presumed 137) + park 120.9s/137/oom 0. Level-15 baseline shutdown ≥120s is systematic, not a fluke. Matches L12 rc pattern (120.9s/137 + 121.0s/137 park), but here on **baseline** at level 15 (higher compression → slower per-file flush than L12 baseline 84.3s).

## STOP verdict (charter STOP + REPORT — NO FURTHER RUNS)

`❌ STOP — b1 baseline: shutdown 121.0s (inferred, driver-died caveat) + park-stop 120.9s/137/oom 0 confirms; 1 `.tmp` orphan (kill-mid-flush, not a failure); 0 `Failed to flush`; `level_confirmed=15`; floor 14≥8 holds.` Single ❌ marks the level per verdict rules. STOP+REPORT triggers FIRED (any SIGTERM ❌) — b2/b3/r1/r2/r3/k1/k3 NOT attempted. Baseline n=1 provisional-❌ (NOT a level median; medians need n=3 — will NOT happen). `run-queue.json` left `done:["1","3","6","9"]` (15 NOT done — deliberate; accepted_partial NOT added — owner decides). State parked: container STOPPED (park-stop 137, never restarted by this agent); shadow jar = baseline `e609c1d4` (no swap occurred — b1 was baseline); tree in `smp-test` volume = post-double-SIGKILL state (any resume MUST pristine-restore first); `stress/level-15/` intact (pristine + small-backup + manifests); CSV holds the b1 ❌ row (committed as data). AWAITING OWNER DIRECTION: (a) accept L15=❌ and close out (tree out, small-world restore, wipe, 27G, accepted_partial), (b) re-run b1 to test fluke (pristine-restore + reboot first), (c) other. Handoff #18: BLOCKED on L15 verdict — L18 MUST NOT start from the L15 tree; needs own convert + pristine discipline if resumed. Medians anchor `bytes_on_disk=13810742340, vs_anvil 0.5300, saved_vs_#1 22.42%`; C1–C7 calibrations in level-01.md (reused, NOT re-derived); L15 tooling notes: host-helper conversion, loopback console via smp-test (`l15wrap.py`), patient-linearstats, host-df fallback, C6b midnight fix holds (future-skipped=0), driver-died-mid-shutdown-wait (log ends 08:23:01, series lost — Loop-5 driver robustness input), nether 65 vs 64 = `.tmp` orphan (kill signature, not staging error).

## CLOSE-OUT (2026-09-24 ~08:45–08:53Z) — owner-accepted ❌ FINAL, sweep ENDING, no re-runs

- Acceptance: owner accepted `test #15 baseline = ❌ FINAL` (shutdown **121.0s** → presumed SIGKILL 137, grace-expiry, oom 0; park-stop replica **120.9s/137/oom 0** confirms 2/2 reproducible). rc at 15 NOT RUN (sweep halted — recorded as not-run, NOT as pass). Baseline n=1 provisional-❌ stands as the level verdict (NOT a median; medians need n=3 — will NOT happen).
- Grading note: **the level is graded complete by verdict, not by row count.** Axis stands at 1 CSV row (b1) + 1 unmeasured park replica; b2/b3/rc/kills will NOT run — deliberate, not missing data.
- Why nothing else ran: charter STOP+REPORT fired on the first SIGTERM ❌. Owner closed L12 as accepted-❌, then ordered no further testing — L12-rc ❌ and L15-baseline ❌ suffice as proof. Sweep ENDS here (no L18, no axis-2, no kills — all moot post-❌).
- Tree: L15 `dimensions/minecraft/{overworld/{region,entities,poi},the_nether/{region,entities},the_end/{region,entities,poi}}` (~13.8G: OW region 13469904602 + OW entities 20034929 + OW poi 1150390 + nether region 132677289 + nether entities 187577 + end region 187672878 + end entities 224460 + end poi 11982) removed from `smp-test` volume (incl. the 1 `.tmp` orphan in nether region — kill-mid-flush signature, gone with the tree; on-disk at close-out `r.0.0.linear.tmp` mtime 08:44Z park window; b1's `r.0.-1` orphan fetched aside as `stress/level-15/dead-b1-r.0.-1.linear.tmp` evidence, wiped with scratch); small world restored from `level-15/smp-test-small-backup` → `smp-test/` back to **1072519 B** (18 small `.linear` files, no poi dirs, 0 `.tmp`; per-dim `data/` + `paper-world.yml` overwritten with backup bytes; world-root `level.dat`/players/`session.lock` likewise restored).
- Configs restored: wholesale copy of the 3 backup files → `compression-level: 15→6` (pre-test 6), `log-flush-batches: true→false`, NMT line removed from `sexidium-node.args` (diff-verified: all 3 files IDENTICAL to backup post-cp).
- Jars: already baseline `e609c1d439e7bdd68e68abccbcf02c818efe77fa4ae8d7ddc2914204ca0ee06b` (no swap ever occurred — b1 was baseline, rc never ran); sha verified post-close-out; node `folia.jar` symlink → shadow untouched.
- Wipe: `stress/level-15/` removed entirely (tree manifests, pristine, EXPECTED host copies, small-backup, helper scripts, dead-b1 evidence — mirror holds EXPECTED/commands/sidecars) + `level-15-{audit,convert,forensic,subset,verify2}.log`; `smp-test` logs/cache/crash-reports wiped.
- Disk: free **14G → 27G** (28±1G band ✓); floor 8G held throughout (minimum observed 14G). Container left STOPPED (never restarted by this agent). Live stack untouched (all live nodes STOPPED by owner; only `stress/*` + `smp-test` volume ever written).
- `run-queue.json`: `done` stays `["1","3","6","9"]`; `accepted_partial:["12","15"]` added host + mirror (this close-out).

## FINAL verdict (owner-accepted, no re-run — SWEEP OVER)

- **`❌ FAIL FINAL — test #15 baseline: shutdown 121.0s (120s grace-expiry, presumed SIGKILL 137, oom 0; driver-died caveat, park-stop 120.9s/137/oom 0 confirms 2/2 reproducible); chunks_lost=-1 (unknown — verify never done before restart, tree double-killed; S2 tail-loss expected but unmeasured).`** rc at 15 NOT RUN (sweep halted — not-run, not pass). b2/b3/kills will NOT run.
- Handoff: NONE — sweep ended by owner order. State handed over clean: `smp-test` holds only the small world, configs at pre-test values, jars at baseline, container STOPPED, free 27G. Medians anchor `bytes_on_disk=13810742340, vs_anvil 0.5300, saved_vs_#1 22.42%`; baseline SIGTERM shutdown 121.0s (n=1); C1–C7 calibrations in level-01.md (reused, NOT re-derived).

## Loop-5 inputs (from L15)

- A1 SOURCE-CORRUPT r.14.16 (converter `Error -3`, exactly one bad chunk slot 517, mtime 2026-09-19 predates campaign; no recovery path) + A2 UNSTABLE-READ r.-17.-8 (manifest src_sha ≠ current bytes, yet convert-time output decodes clean; possible host read-path instability — cross-ref agent-7 memory-corruption finding, memtest never run) + A3 converter EXIT=0 despite `errors=1` (DONE-line authoritative, not exit code).
- Driver-died-mid-shutdown-wait (runner log ends 08:23:01Z, mem-series + verify lost; driver writes mirror-direct so EDITLOGs/panels/tail survived) — driver robustness input.
- `.tmp` orphan naming: b1 kill left `r.0.-1` (fetched aside), park kill left `r.0.0` (mtime 08:44Z) — per-kill orphans differ; both nether-region kill-mid-flush signatures, neither a failure.
- Re-confirms (already logged L12): C6b midnight-date-line parse fix holds (future-skipped=0); host-helper conversion + loopback console + patient-linearstats + host-df fallback discipline.

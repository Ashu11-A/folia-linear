# Ship default - paste into paper-world-defaults.yml at the top level,
# NOT under `chunks`.
#
# Anvil stays the default for every world; Linear is opt-in per world via
# paper-world.region-format.yml.
region-format:
  format: ANVIL
  linear:
    compression-level: 1
    crash-on-broken-symlink: true
